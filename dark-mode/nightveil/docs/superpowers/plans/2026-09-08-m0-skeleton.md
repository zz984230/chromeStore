# NightVeil M0（项目骨架）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭起 NightVeil 的项目地基——esbuild 构建链、程序化图标、manifest 骨架、最小可加载空壳，最终在 /tabbit 中通过 chrome://extensions 无报错加载验收。

**Architecture:** 源码 `src/` 以 ES Modules 编写（分层：background / content / options / shared），esbuild 打包为 IIFE bundle 输出到 `extension/`；静态资产（manifest、HTML、图标）放 `public/`，构建时原样拷入。图标用零依赖 Node 脚本程序化生成 PNG。验收页由零依赖静态服务托管于 localhost:8123。

**Tech Stack:** Node.js ≥ 18.17（node:test、fs.cp）、esbuild（唯一 devDependency）、Chrome MV3、/tabbit 验证。

## Global Constraints

- 工作目录：`/Users/zero/Project/chromeStore/dark-mode/nightveil`（git 仓库根在上一级，所有 git 命令在本目录执行即可）
- Node ≥ 18.17（package.json 已声明 engines）
- 防拷贝红线（docs/adr/0003）：实现任何代码时不得打开原版插件源码参照；规格来源只有本计划与行为清单
- 图标必须程序化生成，禁止复用/模仿原版 PNG 文件
- 所有用户可见文案为英文，且只能来自 `src/shared/strings.js`（集中管理）
- `extension/` 是构建产物：不手改、已进 .gitignore
- manifest 权限集固定为 `permissions: ["storage", "contextMenus"]`、`optional_permissions: ["alarms"]`、`host_permissions: ["<all_urls>"]`；任何变更须先征得用户同意（自主迭代.md）
- 无 popup：`action` 只有 default_title/default_icon，不挂 popup 页面
- CSS 变量前缀 `--nv-*`（本里程碑暂不使用，先立规矩）
- esbuild 安装失败（版本号不存在）时：`npm i -D esbuild@latest` 并把 package.json 中版本同步为实际安装版本，视为该步骤内修复而非偏离计划
- 提交信息用 conventional commits，末尾加 `Co-Authored-By: Claude Code <noreply@anthropic.com>`

---

### Task 1: 项目地基（package.json / .gitignore / 安装依赖 / 文档入库）

**Files:**
- Create: `.gitignore`
- Create: `package.json`

**Interfaces:**
- Consumes: 无（首个任务）
- Produces: npm scripts `build` / `watch` / `fixtures` / `icons` / `test`（后续任务的命令入口）；`node_modules/` 与 esbuild 可执行

- [ ] **Step 1: 写 .gitignore**

```gitignore
node_modules/
extension/
.DS_Store
*.log
```

- [ ] **Step 2: 写 package.json**

```json
{
  "name": "nightveil",
  "private": true,
  "version": "0.1.0",
  "description": "A global dark theme for the web.",
  "type": "module",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "fixtures": "node scripts/serve-fixtures.mjs",
    "icons": "node scripts/gen-icons.mjs",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
```

- [ ] **Step 3: 安装依赖**

Run: `npm install`
Expected: 生成 `package-lock.json` 与 `node_modules/`，退出码 0。（若 esbuild ^0.25.0 不存在，按 Global Constraints 的回退处理）

- [ ] **Step 4: 验证 esbuild 就位**

Run: `npx esbuild --version`
Expected: 输出版本号（如 `0.25.x`），退出码 0

- [ ] **Step 5: 提交（含此前 grilling 阶段产出的共识文档）**

```bash
git add .gitignore package.json package-lock.json CONTEXT.md 自主迭代.md docs/
git commit -m "chore: nightveil M0 project base (package.json, gitignore, consensus docs)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: 程序化图标生成器（TDD）

**Files:**
- Test: `tests/unit/icons.test.mjs`
- Create: `scripts/gen-icons.mjs`
- Create（由脚本生成）: `public/icons/{toolbar,dark,light}/{16,32,48,64,128}.png`（15 个文件）

**Interfaces:**
- Consumes: Task 1 的 `npm run icons` 入口
- Produces: `public/icons/<set>/<size>.png` 路径约定，Task 3 的 manifest 将引用 `icons/toolbar/16|32|48|64.png` 与 `icons/16|32|48|128.png`（app 图标复用 toolbar 集）；三套配色 = 三种状态（toolbar 中性、dark 深底、light 浅底）

- [ ] **Step 1: 写失败测试**

```js
// tests/unit/icons.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { execFileSync } from 'node:child_process';
import { readFileSync } from 'node:fs';

const PNG_SIGNATURE = Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]);
const SETS = ['toolbar', 'dark', 'light'];
const SIZES = [16, 32, 48, 64, 128];

test('icons regenerate as valid square PNGs for every set and size', () => {
  execFileSync(process.execPath, ['scripts/gen-icons.mjs']);
  for (const set of SETS) {
    for (const size of SIZES) {
      const buf = readFileSync(`public/icons/${set}/${size}.png`);
      assert.deepEqual(buf.subarray(0, 8), PNG_SIGNATURE, `${set}/${size} PNG signature`);
      assert.equal(buf.readUInt32BE(16), size, `${set}/${size} IHDR width`);
      assert.equal(buf.readUInt32BE(20), size, `${set}/${size} IHDR height`);
    }
  }
});

test('dark and light state icons are visually distinct files', () => {
  assert.notDeepEqual(
    readFileSync('public/icons/dark/64.png'),
    readFileSync('public/icons/light/64.png'),
  );
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/unit/icons.test.mjs`
Expected: FAIL（`scripts/gen-icons.mjs` 不存在，execFileSync 抛 ENOENT）

- [ ] **Step 3: 实现生成器**

```js
// scripts/gen-icons.mjs
// Generates all NightVeil icons as PNGs (zero dependencies).
// Output: public/icons/{toolbar,dark,light}/{16,32,48,64,128}.png
// Design: rounded-square plate + crescent moon, 4x4 supersampled antialiasing.
import { mkdirSync, writeFileSync } from 'node:fs';
import { deflateSync } from 'node:zlib';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const SIZES = [16, 32, 48, 64, 128];

// set name -> [plate RGB, moon RGB]
const SETS = {
  toolbar: [[90, 95, 107], [242, 242, 242]],
  dark: [[30, 34, 48], [232, 226, 212]],
  light: [[233, 233, 236], [58, 63, 74]],
};

// ---------- PNG encoding ----------
const CRC_TABLE = Array.from({ length: 256 }, (_, n) => {
  let c = n;
  for (let k = 0; k < 8; k++) c = c & 1 ? 0xedb88320 ^ (c >>> 1) : c >>> 1;
  return c >>> 0;
});

function crc32(buf) {
  let c = 0xffffffff;
  for (const byte of buf) c = CRC_TABLE[(c ^ byte) & 0xff] ^ (c >>> 8);
  return (c ^ 0xffffffff) >>> 0;
}

function chunk(type, data) {
  const typeBytes = Buffer.from(type, 'ascii');
  const length = Buffer.alloc(4);
  length.writeUInt32BE(data.length);
  const crc = Buffer.alloc(4);
  crc.writeUInt32BE(crc32(Buffer.concat([typeBytes, data])));
  return Buffer.concat([length, typeBytes, data, crc]);
}

function encodePng(size, rgba) {
  const ihdr = Buffer.alloc(13);
  ihdr.writeUInt32BE(size, 0); // width
  ihdr.writeUInt32BE(size, 4); // height
  ihdr[8] = 8;  // bit depth
  ihdr[9] = 6;  // color type RGBA
  const stride = size * 4;
  const raw = Buffer.alloc((stride + 1) * size);
  for (let y = 0; y < size; y++) {
    raw[y * (stride + 1)] = 0; // filter: none
    rgba.copy(raw, y * (stride + 1) + 1, y * stride, (y + 1) * stride);
  }
  return Buffer.concat([
    Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]),
    chunk('IHDR', ihdr),
    chunk('IDAT', deflateSync(raw)),
    chunk('IEND', Buffer.alloc(0)),
  ]);
}

// ---------- drawing (unit-square coordinates) ----------
function sdRoundRect(px, py, half, r) {
  const qx = Math.abs(px) - half + r;
  const qy = Math.abs(py) - half + r;
  return Math.min(Math.max(qx, qy), 0) + Math.hypot(Math.max(qx, 0), Math.max(qy, 0)) - r;
}

const MOON = { cx: 0.42, cy: 0.5, r: 0.3 };
const BITE = { cx: 0.66, cy: 0.36, r: 0.27 };

function inCircle(x, y, c) {
  return Math.hypot(x - c.cx, y - c.cy) < c.r;
}

function isMoon(x, y) {
  return inCircle(x, y, MOON) && !inCircle(x, y, BITE);
}

function render(size, [plate, moon]) {
  const SS = 4; // supersample factor per axis
  const rgba = Buffer.alloc(size * size * 4);
  for (let py = 0; py < size; py++) {
    for (let px = 0; px < size; px++) {
      let onPlate = 0;
      let onMoon = 0;
      for (let sy = 0; sy < SS; sy++) {
        for (let sx = 0; sx < SS; sx++) {
          const x = (px + (sx + 0.5) / SS) / size;
          const y = (py + (sy + 0.5) / SS) / size;
          if (sdRoundRect(x - 0.5, y - 0.5, 0.48, 0.22) < 0) {
            onPlate++;
            if (isMoon(x, y)) onMoon++;
          }
        }
      }
      const total = SS * SS;
      const i = (py * size + px) * 4;
      const aPlate = Math.round((onPlate / total) * 255);
      const aMoon = Math.round((onMoon / total) * 255);
      const [r, g, b] = aMoon > 0 ? moon : plate;
      rgba[i] = r;
      rgba[i + 1] = g;
      rgba[i + 2] = b;
      rgba[i + 3] = Math.max(aPlate, aMoon);
    }
  }
  return encodePng(size, rgba);
}

const outRoot = path.join(path.dirname(fileURLToPath(import.meta.url)), '..', 'public', 'icons');
for (const [set, palette] of Object.entries(SETS)) {
  const dir = path.join(outRoot, set);
  mkdirSync(dir, { recursive: true });
  for (const size of SIZES) writeFileSync(path.join(dir, `${size}.png`), render(size, palette));
}
console.log(`[nightveil] icons written to public/icons (${Object.keys(SETS).join(', ')} x ${SIZES.join(', ')})`);
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test tests/unit/icons.test.mjs`
Expected: 2 pass, 0 fail

- [ ] **Step 5: 肉眼抽查最大尺寸图标（防"测试通过但画错了"）**

Run: `open public/icons/dark/128.png`
Expected: 深蓝底圆角方块 + 米白月牙；若图形残缺/错位，调整 `MOON`/`BITE`/`sdRoundRect` 参数重跑 Step 4

- [ ] **Step 6: 提交**

```bash
git add scripts/gen-icons.mjs tests/unit/icons.test.mjs public/icons/
git commit -m "feat: programmatic PNG icon generator with crescent design

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: 构建链 + 源码空壳 + manifest（TDD·构建冒烟）

**Files:**
- Test: `tests/build.test.mjs`
- Create: `scripts/build.mjs`
- Create: `src/shared/strings.js`
- Create: `src/background/main.js`
- Create: `src/content/main.js`
- Create: `src/options/main.js`
- Create: `public/manifest.json`
- Create: `public/options.html`

**Interfaces:**
- Consumes: Task 1 的 npm scripts；Task 2 的 `public/icons/**`（manifest 引用）
- Produces: `extension/` 完整布局 `{manifest.json, background.js, content.js, options.js, options.html, icons/**}`——Task 5 的 /tabbit 验收加载对象；`STRINGS` 命名导出（`src/shared/strings.js`），键：`extensionName` / `swStartedDebug` / `contentActiveLog` / `optionsHeading` / `optionsNote`

- [ ] **Step 1: 写失败测试**

```js
// tests/build.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { execFileSync } from 'node:child_process';
import { existsSync, readFileSync } from 'node:fs';

test('build produces a complete loadable extension directory', () => {
  execFileSync(process.execPath, ['scripts/build.mjs']);
  const files = [
    'extension/manifest.json',
    'extension/background.js',
    'extension/content.js',
    'extension/options.js',
    'extension/options.html',
    'extension/icons/toolbar/16.png',
    'extension/icons/dark/128.png',
  ];
  for (const f of files) assert.ok(existsSync(f), `missing ${f}`);

  const manifest = JSON.parse(readFileSync('extension/manifest.json', 'utf8'));
  assert.equal(manifest.manifest_version, 3);
  assert.equal(manifest.name, 'NightVeil');
  assert.deepEqual(manifest.permissions, ['storage', 'contextMenus']);
  assert.deepEqual(manifest.optional_permissions, ['alarms']);
  assert.deepEqual(manifest.host_permissions, ['<all_urls>']);
  assert.equal(manifest.action.default_popup, undefined, 'no popup by design');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/build.test.mjs`
Expected: FAIL（`scripts/build.mjs` 不存在）

- [ ] **Step 3: 写集中文案模块**

```js
// src/shared/strings.js
// All user-visible English copy lives here (i18n-ready per ADR-0003).
export const STRINGS = Object.freeze({
  extensionName: 'NightVeil',
  swStartedDebug: '[NightVeil] service worker started',
  contentActiveLog: '[NightVeil] content script active',
  optionsHeading: 'NightVeil Options',
  optionsNote: 'Settings arrive in Milestone 1.',
});
```

- [ ] **Step 4: 写三个入口（空壳）**

```js
// src/background/main.js
// Service worker entry. State management, menus, tab sync land in M1.
import { STRINGS } from '../shared/strings.js';

console.debug(STRINGS.swStartedDebug);
```

```js
// src/content/main.js
// Content script entry (document_start, all frames). Theming starts in M1.
import { STRINGS } from '../shared/strings.js';

console.log(STRINGS.contentActiveLog);
```

```js
// src/options/main.js
import { STRINGS } from '../shared/strings.js';

document.querySelector('#heading').textContent = STRINGS.optionsHeading;
document.querySelector('#note').textContent = STRINGS.optionsNote;
```

- [ ] **Step 5: 写 manifest 与 options 占位页**

```json
// public/manifest.json
{
  "manifest_version": 3,
  "name": "NightVeil",
  "version": "0.1.0",
  "description": "A global dark theme for the web.",
  "icons": {
    "16": "icons/toolbar/16.png",
    "32": "icons/toolbar/32.png",
    "48": "icons/toolbar/48.png",
    "128": "icons/toolbar/128.png"
  },
  "action": {
    "default_title": "NightVeil",
    "default_icon": {
      "16": "icons/toolbar/16.png",
      "32": "icons/toolbar/32.png",
      "48": "icons/toolbar/48.png",
      "64": "icons/toolbar/64.png"
    }
  },
  "background": {
    "service_worker": "background.js"
  },
  "options_ui": {
    "open_in_tab": true,
    "page": "options.html"
  },
  "permissions": ["storage", "contextMenus"],
  "optional_permissions": ["alarms"],
  "host_permissions": ["<all_urls>"],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_start",
      "all_frames": true,
      "match_about_blank": true
    }
  ]
}
```

```html
<!-- public/options.html -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>NightVeil</title>
  </head>
  <body>
    <main>
      <h1 id="heading"></h1>
      <p id="note"></p>
    </main>
    <script src="options.js"></script>
  </body>
</html>
```

- [ ] **Step 6: 写构建脚本**

```js
// scripts/build.mjs
// Bundles src/ (ES modules) into extension/ and copies public/ verbatim.
// --watch: rebuild JS on change, re-copy public/ on change.
import { build, context } from 'esbuild';
import { cp, rm } from 'node:fs/promises';
import { watch } from 'node:fs';
import process from 'node:process';

const WATCH = process.argv.includes('--watch');

const OPTIONS = {
  entryPoints: {
    background: 'src/background/main.js',
    content: 'src/content/main.js',
    options: 'src/options/main.js',
  },
  outdir: 'extension',
  bundle: true,
  format: 'iife',
  target: ['chrome120'],
  sourcemap: false,
  logLevel: 'info',
};

await rm('extension', { recursive: true, force: true });

if (!WATCH) {
  await build(OPTIONS);
  await cp('public', 'extension', { recursive: true });
} else {
  const ctx = await context(OPTIONS);
  await ctx.watch();
  await cp('public', 'extension', { recursive: true });
  watch('public', { recursive: true }, () => {
    cp('public', 'extension', { recursive: true }).catch(() => {});
  });
  console.log('[nightveil] watching src/ and public/ ...');
}
```

- [ ] **Step 7: 运行测试确认通过**

Run: `node --test tests/build.test.mjs`
Expected: 1 pass（extension/ 完整产出，manifest 断言全过）

- [ ] **Step 8: 全量测试回归**

Run: `npm test`
Expected: icons 2 pass + build 1 pass = 3 pass, 0 fail

- [ ] **Step 9: 提交**

```bash
git add scripts/build.mjs src/ public/ tests/build.test.mjs
git commit -m "feat: esbuild pipeline, MV3 manifest skeleton, shell entries

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: 验收页静态服务（TDD）

**Files:**
- Test: `tests/unit/server.test.mjs`
- Create: `scripts/serve-fixtures.mjs`
- Create: `tests/fixtures/index.html`（M0 占位页，M1 扩成 6 页套件）

**Interfaces:**
- Consumes: Task 1 的 `npm run fixtures` 入口
- Produces: `http://localhost:8123/`（目录索引指向 index.html）——Task 5 与 M1 起所有 /tabbit 页面级验收的目标页面基址

- [ ] **Step 1: 写失败测试**

```js
// tests/unit/server.test.mjs
import test from 'node:test';
import assert from 'node:assert/strict';
import { spawn } from 'node:child_process';

test('fixtures server serves index.html and 404s unknown paths', async () => {
  const child = spawn(process.execPath, ['scripts/serve-fixtures.mjs'], { stdio: 'ignore' });
  try {
    let res;
    for (let i = 0; i < 20 && !res; i++) {
      try {
        res = await fetch('http://localhost:8123/');
      } catch {
        await new Promise((r) => setTimeout(r, 150));
      }
    }
    assert.ok(res, 'server never came up');
    assert.equal(res.status, 200);
    assert.match(res.headers.get('content-type'), /text\/html/);
    assert.match(await res.text(), /Fixture stub/);

    const missing = await fetch('http://localhost:8123/nope.html');
    assert.equal(missing.status, 404);
  } finally {
    child.kill();
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test tests/unit/server.test.mjs`
Expected: FAIL（`scripts/serve-fixtures.mjs` 不存在，server never came up）

- [ ] **Step 3: 写占位验收页**

```html
<!-- tests/fixtures/index.html -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>NightVeil fixture</title>
  </head>
  <body style="background: #fff; color: #111">
    <h1>Fixture stub</h1>
    <p>Full fixture suite lands in Milestone 1.</p>
  </body>
</html>
```

- [ ] **Step 4: 写静态服务**

```js
// scripts/serve-fixtures.mjs
// Zero-dependency static server for acceptance fixtures (port 8123).
import { createServer } from 'node:http';
import { readFile } from 'node:fs/promises';
import path from 'node:path';

const ROOT = path.resolve('tests/fixtures');
const PORT = 8123;
const TYPES = {
  '.html': 'text/html; charset=utf-8',
  '.css': 'text/css; charset=utf-8',
  '.js': 'text/javascript; charset=utf-8',
  '.png': 'image/png',
};

const server = createServer(async (req, res) => {
  try {
    const urlPath = decodeURIComponent(new URL(req.url, 'http://localhost').pathname);
    const filePath = path.normalize(path.join(ROOT, urlPath === '/' ? 'index.html' : urlPath));
    if (!filePath.startsWith(ROOT)) {
      res.writeHead(403);
      return res.end();
    }
    const body = await readFile(filePath);
    res.writeHead(200, { 'content-type': TYPES[path.extname(filePath)] ?? 'application/octet-stream' });
    res.end(body);
  } catch {
    res.writeHead(404);
    res.end('not found');
  }
});

server.listen(PORT, () => console.log(`[nightveil] fixtures on http://localhost:${PORT}`));
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test tests/unit/server.test.mjs`
Expected: 1 pass

- [ ] **Step 6: 全量测试回归**

Run: `npm test`
Expected: 4 pass, 0 fail（icons 2 + build 1 + server 1）

- [ ] **Step 7: 提交**

```bash
git add scripts/serve-fixtures.mjs tests/unit/server.test.mjs tests/fixtures/index.html
git commit -m "feat: localhost fixtures server for tabbit acceptance

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 5: /tabbit 加载验收 + 里程碑收口

**Files:**
- Modify: `docs/MILESTONES.md`（勾选 M0 全部条目、总览 M0 → ✅、更新日期）
- 无新代码

**Interfaces:**
- Consumes: Task 3 的 `extension/`、Task 4 的 `http://localhost:8123/`
- Produces: M0 验收通过的状态记录（后续里程碑依赖该基线）

- [ ] **Step 1: 构建并启动 fixtures 服务**

Run: `npm run build`（Expected: extension/ 重建，3 个 entry 输出）
Run（后台）: `npm run fixtures`
Expected: `[nightveil] fixtures on http://localhost:8123`

- [ ] **Step 2: 调用 /tabbit 执行加载验收**

调用 tabbit 技能，指令要点：
1. 以"加载已解压的扩展程序"方式装入 `<项目>/extension/`
2. 截图 `chrome://extensions` 的 NightVeil 卡片——Expected: 无"错误"红色按钮、无黄色警告条
3. 打开 `http://localhost:8123/`，读取页面 console——Expected: 出现 `[NightVeil] content script active`，无未捕获异常
4. 打开 NightVeil 的 service worker 控制台（扩展卡片 → "Service Worker"链接）——Expected: 出现 `[NightVeil] service worker started`，无报错
5. 右键工具栏图标 → 选项，截图 options 页——Expected: 标题 "NightVeil Options"、说明段渲染正常

- [ ] **Step 3: 对照验收标准判定**

逐条对照 `自主迭代.md` 通用条目 1~3（第 4 条回归测试本轮即 `npm test` 全绿，Step 1 前已跑）：
- 全部通过 → 进入 Step 4
- 任一失败 → 按自主迭代协议回到代码修改，同一问题连续 3 次失败即停止上报

- [ ] **Step 4: 更新里程碑文档**

`docs/MILESTONES.md`：M0 五个条目全部勾选 `[x]`；总览表 M0 状态 🟨 → ✅，验收轮次填 `/tabbit 1 轮通过`；更新顶部日期。

- [ ] **Step 5: 提交收口**

```bash
git add docs/MILESTONES.md
git commit -m "docs: M0 accepted via tabbit (loads clean, shell verified)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Self-Review 记录

- **覆盖核对**（对照 MILESTONES.md M0 清单）：目录结构/.gitignore→T1；三命令→T1(icons/build/watch)+T4(fixtures)；manifest 权限骨架→T3；图标→T2；最小空壳+/tabbit 加载→T3+T5。无缺口。
- **占位符扫描**：无 TBD/TODO/"适当处理"；所有代码步骤含完整代码，验证步骤含命令与预期输出。
- **类型/命名一致性**：`STRINGS` 键在 T3 定义与消费一致；图标路径（`icons/toolbar/*`、`icons/dark/128.png`）在 T2 产出、T3 manifest 引用、T5 不涉及；入口名 `background/content/options` 在 build.mjs、manifest、options.html `<script src="options.js">` 三处一致。
- **已知取舍**：app 图标与 toolbar 默认图标复用同一套（三套状态图留给 M1 按钮状态切换用）；fixtures M0 只有一张占位页（M1 扩为 6 页）。
